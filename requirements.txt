# --- Layer 1 (baseline RAG) ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
rank-bm25>=0.2.2
pypdf>=4.0.0
python-dotenv>=1.0.0

# --- Layer 2 (CRAG) ---
tavily-python>=0.5.0          # web-search fallback

# --- Layer 5: eval harness ---
# ragas <=0.4.3 crashes on import next to langchain-community 0.4.x (it imports
# the removed vertexai module); eval/run_eval.py injects a small shim before
# importing ragas. Pinned so a future ragas release doesn't silently change
# metric definitions under the README's numbers. `datasets` comes in as a
# ragas dependency.
ragas==0.4.3

# --- Layer 5: serving ---
fastapi>=0.110.0
uvicorn>=0.29.0

# --- Added in later layers (install when you reach them) ---
# langsmith>=0.1.0            # Layer 5: observability/tracing
