# Runtime dependencies — pinned to the exact versions the working venv runs.
#
# WHY EXACT PINS: the previous `>=` floors (langchain>=0.3.0, langgraph>=0.2.0)
# were two major versions behind reality and had no ceilings, so pip had to
# solve across the whole released history of both packages. It backtracked for
# seven minutes and gave up with ResolutionImpossible: every modern `langchain`
# requires `langgraph<1.3.0`, while an unbounded `langgraph>=0.2.0` kept pulling
# a newer one. Exact pins remove the search entirely.
#
# Regenerate after any intentional upgrade; do not hand-edit a version here
# without upgrading the venv to match.

# --- LangGraph orchestration ---
langgraph==1.2.7
langgraph-checkpoint-sqlite==3.1.0        # Layer 4: SqliteSaver

# --- LangChain components (only these are imported by src/) ---
langchain-core==1.4.8
langchain-community==0.4.2                # PyPDFLoader, TextLoader, BM25Retriever
langchain-openai==1.3.3
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
                         # NOT imported anywhere in src/ — kept
                                          # pinned because langchain-classic is in
                                          # the working env and may be pulled
                                          # through it. Try dropping on Day 2 and
                                          # confirm the app still boots.

# --- Retrieval + ingestion ---
chromadb==1.5.9
rank-bm25==0.2.2
pypdf==6.14.2

# --- Layer 2: web-search fallback ---
tavily-python==0.7.26

# --- Serving ---
fastapi==0.140.7
uvicorn==0.49.0
pydantic==2.13.4                          # imported directly in app.py
pydantic-settings==2.15.0                 # imported directly in src/config.py
python-dotenv==1.2.2
